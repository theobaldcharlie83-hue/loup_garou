# ⚠️ Actuellement INUTILISÉ par l'application.
# Le moteur de décision des peluches est 100 % local (src/services/) : aucune clé
# d'API n'est requise pour faire fonctionner le jeu.
#
# IMPORTANT : toute variable préfixée par VITE_ est embarquée EN CLAIR dans le bundle
# client (et donc publiquement exposée sur GitHub Pages). N'y mettez jamais un secret
# destiné à être appelé directement depuis le navigateur.
#
# VITE_GEMINI_API_KEY=your_gemini_api_key_here
